
# http://www.pythondoc.com/pythontutorial3

# https://github.com/python

# https://docs.python.org/zh-cn/3/index.html

# Python 3.7.2 最终版 ；发布日期: 2018-12-23

==========    install module  ==================
python3.4/5/6.7

python -m pip install will install a package just for the current user

python -m pip install SomePackage==1.0.4    # specific version
python -m pip install "SomePackage>=1.0.4"  # minimum version
python -m pip install --upgrade SomePackage

pip install -r require.txt

easy_install  xxxx

Extending and Embedding the Python Interpreter

This document describes how to write modules in C or C++ to extend the Python interpreter with new modules.

每个Python模块由.pyi “存根” 表示。这是一个正常的 Python文件（即它可以被Python 3解释

在'Mypy'库中，他们明确提到“存根”文件作为公共接口

一个存根文件仅包含公共接口的 模块的描述没有任何实现。

























